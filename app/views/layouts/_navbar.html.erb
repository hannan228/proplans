<style>
  .nav_danger_btn{
      color: white;
      text-decoration: none
  }
  .nav_danger_btn:hover {
    color: white;
  }
</style>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">All Network Package</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
        data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
        aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse row" id="navbarSupportedContent">
        <div class="col-md-6">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <% if current_page?('/') %>
                <%# if home page is active then %>
                <a class="nav-link active" aria-current="page" href="/">Home</a>
              <% else %>
                <%# if home page is not active then %>
                <a class="nav-link"  aria-current="page" href="/">Home</a>
              <% end %>
            </li> 

            <li class="nav-item">
              <% if current_page?('/projects') %>
                <%# if prroject page is active then %>
                <a class="nav-link active" href="/packages">Packages</a>
              <% else %>
                <%# if prroject page is not active then %>
                <a class="nav-link"  aria-current="page" href="/packages">Packages</a>
              <% end %>
            </li>
          </ul>
        </div>
        <div class="col-md-6">
          <%# <form class="d-flex">
            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-light" type="submit">Search</button>
          </form> %>
          <div class="d-grid ms-2 gap-2 d-md-block">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <% if @current_user %>
                <li><%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: "nav_danger_btn btn btn-danger" %></li>
              <% else %>
                <li><%= link_to "Sign Up", new_user_path,  class: "nav_danger_btn btn btn-primary" %><li>
                <li><%= link_to "Login", new_user_session_path,  class: "nav_danger_btn btn btn-primary" %><li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
	</nav>
<body>
